'use strict';

/**
 * image-carousel controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::image-carousel.image-carousel');
